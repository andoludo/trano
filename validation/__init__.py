"""Validation suites for the Trano package.

Each subpackage hosts an end-to-end benchmark (model generation, simulation,
KPI extraction, comparison against published reference data, report rendering).
These suites are not unit tests — they produce reports under ``_reports/``.
"""
